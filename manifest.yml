---
applications:
- name: rails_sample
  memory: 256M
  instances: 1
  host: rails_sample_${random-word}
  domain: cfapps.io
  path: .
  command: bundle exec rake db:migrate && bundle exec rails s -p $PORT
  services:
    rails_postgres:
      label: elephantsql
      provider: elephantsql
      version: n/a
      plan: turtle
